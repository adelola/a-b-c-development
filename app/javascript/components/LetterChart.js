import React from 'react';
import Column from './../images/column.svg'

const LetterChart = (props) => {
    const answerArray =  props.letter

    return( 
      <Column height={500} width={20} />
    )
}

export default LetterChart